<% content_for :head do %>
  <%= display_meta_tags site: 'Venshop', title: "Cart" %>
  <%= display_meta_tags keywords: %w[venshop Venshop cart amazon ] %>
<% end %>
<div class="container">
  <div class="row">
    <div class="col-md-offset-0 col-md-4">
      <%= link_to "Home", root_path%> > My cart
    </div>
  </div>
  <br />
  <div class="row">
    <div class="col-md-offset-0 col-md-3">
      <%= render "categories/catalog", { categories: @categories}%>
    </div>
    <div class="col-md-offset-0 col-md-9">
      <div>
        <%= render "carts/cart_items", { cart_items: @cart_items,
          count_items: @count_items, subtotal: @subtotal, reload: @reload }%>
      </div>
    </div>
  </div>
</div>
